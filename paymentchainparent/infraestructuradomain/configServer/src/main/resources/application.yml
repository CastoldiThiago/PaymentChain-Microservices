server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/CastoldiThiago/config-data-paymentchain.git
          default-label: master

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-server:8761/eureka/